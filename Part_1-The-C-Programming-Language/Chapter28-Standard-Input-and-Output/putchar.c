/*  putchar -   The putchar function outputs/write a character to the standard output. The function is declared inside a <stdio.h> header and has the following syntax:
 *
 *      int putchar (int ch);
 *
 * To write a single character to the standard output, we use:
 */

#include <stdio.h>

int main(void)
{
    char c = 'A';
    putchar(c);
}
